import axios from 'axios'

// eslint-disable-next-line import/prefer-default-export
export const saveDataTenant = async (tenantData) => {
  // const response = await axios.post('/api/database', ownerData)
  return null
}
